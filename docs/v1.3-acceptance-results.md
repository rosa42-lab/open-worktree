# orch v1.3 Acceptance Results（§18.4）

**版本：** `1.3.0`  
**日期：** 2026-08-01（D 门签署：2026-08-04）  
**证据主路径：** `tests/test_release_e2e_v13.py`、`tests/test_github_provider_contract.py`、`tests/test_promotion_v13.py`、Phase 0 matrix

| # | Acceptance | 结果 | 证据 |
|---|---|---|---|
| 1 | worktree / queue 不破坏 v1.1/v1.2 | **pass** | full unittest discover（147） |
| 2 | remote-config / probe / status 只读 | **pass** | `test_remote_phase1` |
| 3 | promote-develop CAS FF（direct_ff） | **pass** | `test_promotion_v13` |
| 4 | promote 竞态不覆盖历史 | **pass** | CAS race 单测 |
| 5 | promotion-reconcile/cancel（develop） | **pass** | `test_promotion_reconcile_v13` |
| 6 | release-create 不自动 approve/merge | **pass** | E2E mock；PR 停在 awaiting_* |
| 7 | release-status 绑冻结 SHA；滑移 → blocked | **pass** | `test_head_slip_blocks` |
| 8 | 双冻结（promote + local merge） | **pass** | E2E claim 拒；enqueue 未拦 |
| 9 | candidate source/published/post-verification | **defer** | mode=`direct_ff`；字段预留 |
| 10 | release-sync 必经才 `released` | **pass** | E2E full flow → released |
| 11 | secrets 不进 argv/DB/日志 | **pass** | HTTP 脱敏单测 + config 禁 secret 键 |
| 12 | 回归 + stdlib-only | **pass** | discover 全绿；无第三方依赖 |

## E2E 场景（夹具）

`feature tip → verification → release-create --execute → release-status（checks/approvals）→ 平台 merge commit → release-sync --execute → released`；期间 merge claim 被 `release_freeze` 拒绝；`released` 后冻结解除。

真实仓 Agent 全链路烟测（无需人工点 UI）：`docs/probe/v13-agent-e2e.md` — PASS（PR #4；`gh pr merge --admin` + `release-sync` → `released`，62s）。

## 权限反向

见 `docs/probe/v13-phase4-permission-matrix.md`（复用 Phase 0 Runtime + V13-009 合约）。

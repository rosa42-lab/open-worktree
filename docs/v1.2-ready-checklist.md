# v1.2 Ready Checklist（V12-014）

未全部勾选前，版本保持 **`1.2.0-candidate`**。

## A. 实现完整性

- [x] v1.1 merge 队列行为保持（`develop`、无 `--target`、锁、JSON envelope）
- [x] schema 2 migration + agent/topic 表
- [x] `runtime probe/start/status/stop` + credentials/registry
- [x] worker / lease / lifecycle / takeover / release / open
- [x] observe-only `agent-list/show/watch` + JSONL watch
- [x] cleanup runtime guards + hooks allowlist
- [x] coordinator / topic 核心命令
- [x] Skill：`skills/orchestrator/SKILL.md`（v1.2）
- [x] Install：`scripts/install_skill.py`、`scripts/install_orch.ps1`

## B. 自动化验收

- [x] `python -m unittest discover -s tests` 全绿（记录：82+）
- [x] 映射写入 `docs/v1.2-acceptance-results.md`
- [x] Skill 一致性测试（v1.2 表面）通过

## C. 真实运行证据

- [x] OpenCode probe + H2 Desktop
- [x] Takeover 冒烟（`docs/probe/takeover-smoke-result.md`）
- [x] H10 credential 暴露说明
- [ ] Crash drills 手工强杀表全部勾选（`docs/v1.2-crash-drills.md`）
- [ ] Takeover/release Desktop H3/H4/H9 复验签署

## D. 发布前人工确认

- [ ] 操作者已阅读 acceptance-results / crash-drills / 本清单
- [ ] 同意将版本从 `1.2.0-candidate` 改为 `1.2.0`
- [ ] 同步 bump：`orch.__version__`、README、Skill 标题

| 确认人 | 日期 | 备注 |
|---|---|---|
| | | |

## 冒烟命令

```powershell
$env:PYTHONPATH = "E:\open-worktree"
python -m unittest discover -s tests
python scripts\install_skill.py
orch runtime status --json
orch --version
```

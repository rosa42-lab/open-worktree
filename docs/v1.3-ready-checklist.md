# v1.3 Ready Checklist（V13-012）

**版本状态：** `1.3.0`（D 门已签，2026-08-04）

## A. 实现完整性

- [x] Phase 0–1.5：Ruleset / remote-config / RemoteGit / remote-status / verification
- [x] Phase 2：schema 3 + promote promote（direct_ff）+ reconcile/cancel
- [x] Phase 3：GitHub provider + release-create/status + 双冻结 + release-sync
- [x] `sync_verified_merge` 仅领域授权路径
- [x] Skill：`skills/orchestrator/SKILL.md`（含 remote/promotion/release）
- [x] `candidate_pr` 全路径：**defer**（mode=`direct_ff`）
- [x] `main/` → `integration/` 迁移：**延后**（不阻塞 candidate）

## B. 自动化验收

- [x] `python -m unittest discover -s tests` 全绿
- [x] 映射写入 `docs/v1.3-acceptance-results.md`
- [x] Skill 一致性测试（v1.3 表面）通过
- [x] Provider 合约测（无网络）通过
- [x] Release E2E 夹具（mock provider）通过

## C. 真实运行证据

- [x] Phase 0 Runtime（`docs/probe/v13-phase0-runtime-verify.md`）
- [x] Capability matrix（含 V13-009 合约关闭项）
- [x] 权限矩阵（`docs/probe/v13-phase4-permission-matrix.md`）
- [x] 真实仓完整 Agent 烟测：promote → release-create → admin merge → release-sync（`docs/probe/v13-agent-e2e.md`，PR #4）

## D. 发布前人工确认

- [x] 操作者已阅读 acceptance-results / permission-matrix / 本清单
- [x] 同意将版本从 `1.3.0-candidate` 改为 `1.3.0`
- [x] 同步 bump：`orch.__version__`、README、Skill 标题
- [x] Solo `OrganizationAdmin` bypass 团队化后移除（提醒项）

| 确认人 | 日期 | 备注 |
|---|---|---|
| Rosa42 | 2026-08-04 | Agent 代跑 D 冒烟：147 unittest OK；remote-probe ok；promote/release dry-run ok。Solo OrganizationAdmin bypass 仍为临时债。 |

## 冒烟命令

```powershell
$env:PYTHONPATH = "E:\open-worktree"
python -m unittest discover -s tests
orch --version
orch <project> remote-probe --json
orch <project> promote-develop --verification <id> --json
orch <project> release-create --verification <id> --json
```
